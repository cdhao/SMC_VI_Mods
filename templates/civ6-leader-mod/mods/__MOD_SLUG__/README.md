# __MOD_NAME__ Runtime Mod

This directory is deployable. It may contain runtime SQL, Lua, ArtDefs, BLP
files, `.dep`, and `.modinfo`; it must not contain source PNG, DDS, TEX, XLP,
Asset Cooker logs, or ModBuddy project files.
