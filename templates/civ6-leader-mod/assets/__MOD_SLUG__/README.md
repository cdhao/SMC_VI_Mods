# __MOD_NAME__ Assets

Keep editable PNG files in `source/`, generated PNG/DDS review files in
`generated/`, and Asset Cooker inputs, XLP files, logs, and temporary BLP files
in `cooker/`. Do not copy any of those build inputs into `mods/__MOD_SLUG__/`.

Read `docs/civ6/asset-pipeline.md` before adding UI textures.
