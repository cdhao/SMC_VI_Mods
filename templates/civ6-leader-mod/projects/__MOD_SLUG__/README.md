# __MOD_NAME__ ModBuddy Project

Create or import the ModBuddy project here if you choose to use ModBuddy. Keep
the project files outside the runtime Mod folder. Configure its asset paths to
use `assets/__MOD_SLUG__/cooker`, not `mods/__MOD_SLUG__/`.
